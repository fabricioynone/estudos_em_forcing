% --- PACOTES BÁSICOS ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[brazilian]{babel}
\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{xcolor}
\usepackage[most]{tcolorbox} % Substitui o mdframed para teoremas
\usepackage{enumitem}
\usepackage{setspace}
\usepackage[a4paper,left=3cm,right=3cm,top=3cm,bottom=3cm]{geometry}
\usepackage{indentfirst}
\usepackage{centernot}

% --- LINE SPACING ---
\onehalfspacing
\allowdisplaybreaks

% --- CORES TEOREMA ---
\definecolor{thmleftline}{RGB}{89, 0, 98} % Azul escuro para a borda esquerda
\definecolor{thmborder}{RGB}{251, 207, 255} % Azul claro para as demais bordas
\definecolor{thmback}{RGB}{254, 245, 255}   % Fundo quase branco

% --- CORES DEFINIÇÃO ---
\definecolor{defleftline}{RGB}{125,174,77}
\definecolor{defborder}{RGB}{218,255,181}
\definecolor{defback}{RGB}{245,250,240}

% --- CONFIGURAÇÃO DE TEOREMAS (amsthm) ---
\theoremstyle{definition}
\newtheorem{theorem}{Teorema}[section] 
\newtheorem{lemma}[theorem]{Lema}
\newtheorem{proposition}[theorem]{Proposição}
\newtheorem{corollary}[theorem]{Corolário}
\newtheorem{exercise}{Exercício}[section]
\theoremstyle{definition}
\newtheorem{definition}{Definição}[section]
\newtheorem{example}{Exemplo}[section]
\theoremstyle{remark}
\newtheorem*{remark}{Observação}

% --- CAIXAS COLORIDAS (tcolorbox envolvendo amsthm) ---
\tcolorboxenvironment{theorem}{
  enhanced,
  breakable,
  colback=thmback,
  colframe=thmborder,
  boxrule=0.5pt,
  leftrule=3pt,
  arc=3pt,
  borderline west={3pt}{0pt}{thmleftline}, % Borda esquerda grossa e escura
  top=4pt, bottom=4pt, left=6pt, right=6pt,
  before skip=12pt, after skip=12pt
}

\tcolorboxenvironment{proposition}{
  enhanced,
  breakable,
  colback=thmback,
  colframe=thmborder,
  boxrule=0.5pt,
  leftrule=3pt,
  arc=3pt,
  borderline west={3pt}{0pt}{thmleftline},
  top=4pt, bottom=4pt, left=6pt, right=6pt,
  before skip=12pt, after skip=12pt
}

\tcolorboxenvironment{corollary}{
  enhanced,
  breakable,
  colback=thmback,
  colframe=thmborder,
  boxrule=0.5pt,
  leftrule=3pt,
  arc=3pt,
  borderline west={3pt}{0pt}{thmleftline},
  top=4pt, bottom=4pt, left=6pt, right=6pt,
  before skip=12pt, after skip=12pt
}



\tcolorboxenvironment{lemma}{
  enhanced,
  breakable,
  colback=thmback,
  colframe=thmborder,
  boxrule=0.5pt,
  leftrule=3pt,
  arc=3pt,
  borderline west={3pt}{0pt}{thmleftline},
  top=4pt, bottom=4pt, left=6pt, right=6pt,
  before skip=12pt, after skip=12pt
}

\tcolorboxenvironment{definition}{
  enhanced,
  breakable,
  parbox=false,
  colback=defback,
  colframe=defborder,
  boxrule=0.5pt,
  leftrule=3pt,
  arc=3pt,
  borderline west={3pt}{0pt}{defleftline},
  top=4pt, bottom=4pt, left=6pt, right=6pt,
  before skip=12pt, after skip=12pt
}


% --- EXERCÍCIOS ---
\newtcolorbox{exercisebox}[1][]{
  enhanced,
  breakable,
  colback=gray!5,
  colframe=gray!50,
  boxrule=1pt,
  arc=5pt,
  title={Exercício #1},
  coltitle=black,
  fonttitle=\bfseries,
  attach boxed title to top left={yshift=-2mm, xshift=5mm},
  boxed title style={colback=gray!15, colframe=gray!50, boxrule=1pt, arc=3pt},
  top=10pt, bottom=10pt, left=10pt, right=10pt,
  before skip=12pt, after skip=12pt
}

\newenvironment{exerciseboxed}[1]
{\begin{exercisebox}[#1]}
{\end{exercisebox}}

% Ambiente para Resolução
\newenvironment{solution}
{\renewcommand{\qedsymbol}{$\blacksquare$}\begin{proof}[Resolução]}
{\end{proof}}

% --- General ---
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\A}{\aleph}
\newcommand{\set}[1]{\left\{ #1 \right\}}
\newcommand{\longlif}{\longleftrightarrow}
\newcommand{\lif}{\leftrightarrow}
\newcommand{\dom}{\operatorname{dom}}

% -- Set Theory -- 
\newcommand{\pset}{\mathcal{P}}
\newcommand{\pred}{\operatorname{pred}}
\newcommand{\ap}{\operatorname{Ap}}

% -- Analysis
\newcommand{\infsum}{\sum_{n=1}^{\infty}}

% -- Not Math
\newcommand{\pending}{\textbf{\textcolor{red}{(PENDING)}}}

% --- NAVEGAÇÃO E LINKS ---
\usepackage{bookmark}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=black,
    bookmarksnumbered=true
}

